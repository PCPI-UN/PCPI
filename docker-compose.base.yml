services:
  # PostgreSQL Databases
  auth-db:
    image: postgres:16-alpine
    container_name: iris-auth-db
    environment:
      POSTGRES_DB: auth_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${AUTH_DB_PASSWORD:-postgres}
    volumes:
      - auth_db_data:/var/lib/postgresql/data
    networks:
      - iris-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d auth_service"]
      interval: 5s
      timeout: 3s 
      retries: 10

  evaluation-db:
    image: postgres:16-alpine
    container_name: iris-evaluation-db
    environment:
      POSTGRES_DB: evaluation_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${EVALUATION_DB_PASSWORD:-postgres}
    volumes:
      - evaluation_db_data:/var/lib/postgresql/data
    networks:
      - iris-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d evaluation_service"]
      interval: 5s
      timeout: 3s
      retries: 10

  event-db:
    image: postgres:16-alpine
    container_name: iris-event-db
    environment:
      POSTGRES_DB: event_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${EVENT_DB_PASSWORD:-postgres}
    volumes:
      - event_db_data:/var/lib/postgresql/data
    networks:
      - iris-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d event_service"]
      interval: 5s
      timeout: 3s
      retries: 10

  invitation-db:
    image: postgres:16-alpine
    container_name: iris-invitation-db
    environment:
      POSTGRES_DB: invitation_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${INVITATION_DB_PASSWORD:-postgres}
    volumes:
      - invitation_db_data:/var/lib/postgresql/data
    networks:
      - iris-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d invitation_service"]
      interval: 5s
      timeout: 3s
      retries: 10

  project-db:
    image: postgres:16-alpine
    container_name: iris-project-db
    environment:
      POSTGRES_DB: project_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${PROJECT_DB_PASSWORD:-postgres}
    volumes:
      - project_db_data:/var/lib/postgresql/data
    networks:
      - iris-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d project_service"]
      interval: 5s
      timeout: 3s
      retries: 10

  # NestJS Services
  auth-service:
    build:
      context: .
      dockerfile: apps/auth-service/Dockerfile
    container_name: iris-auth-service
    depends_on:
      auth-db:
        condition: service_healthy
    networks:
      - iris-network

  evaluation-service:
    build:
      context: .
      dockerfile: apps/evaluation-service/Dockerfile
    container_name: iris-evaluation-service
    depends_on:
      evaluation-db:
        condition: service_healthy
    networks:
      - iris-network

  event-service:
    build:
      context: .
      dockerfile: apps/event-service/Dockerfile
    container_name: iris-event-service
    depends_on:
      event-db:
        condition: service_healthy
    networks:
      - iris-network

  invitation-service:
    build:
      context: .
      dockerfile: apps/invitation-service/Dockerfile
    container_name: iris-invitation-service
    depends_on:
      invitation-db:
        condition: service_healthy
    networks:
      - iris-network

  project-service:
    build:
      context: .
      dockerfile: apps/project-service/Dockerfile
    container_name: iris-project-service
    depends_on:
      project-db:
        condition: service_healthy
    networks:
      - iris-network

  gateway:
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    container_name: iris-gateway
    depends_on:
      - auth-service
      - evaluation-service
      - event-service
      - invitation-service
      - project-service
      - notification-service
    networks:
      - iris-network

  notification-service:
    build:
      context: .
      dockerfile: ./apps/notification-service/Dockerfile
    container_name: iris-notification-service
    networks:
      - iris-network

networks:
  iris-network:
    driver: bridge
    name: iris-network

volumes:
  auth_db_data:
    name: iris-auth-db-data
  evaluation_db_data:
    name: iris-evaluation-db-data
  event_db_data:
    name: iris-event-db-data
  invitation_db_data:
    name: iris-invitation-db-data
  project_db_data:
    name: iris-project-db-data
