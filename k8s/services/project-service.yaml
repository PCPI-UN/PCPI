apiVersion: v1
kind: Service
metadata:
  name: project-service
  namespace: pcpi
spec:
  selector:
    app: project-service
  ports:
    - port: 50055
      targetPort: 50055
      protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-service
  namespace: pcpi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: project-service
  template:
    metadata:
      labels:
        app: project-service
    spec:
      containers:
        - name: project-service
          image: pcpi/project-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50055
          env:
            - name: DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: pcpi-config
                  key: PROJECT_DATABASE_URL
            - name: GRPC_HOST
              valueFrom:
                configMapKeyRef:
                  name: pcpi-config
                  key: PROJECT_GRPC_HOST
            - name: GRPC_PORT
              valueFrom:
                configMapKeyRef:
                  name: pcpi-config
                  key: PROJECT_GRPC_PORT
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: pcpi-config
                  key: NODE_ENV
          readinessProbe:
            tcpSocket:
              port: 50055
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 50055
            initialDelaySeconds: 15
            periodSeconds: 10
