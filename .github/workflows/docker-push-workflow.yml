name: Docker Image CI

on:
  push:
    branches: [ "refactor/CU-86e0d71ar/Dev-Tooling-Investigate-and-document-Dockerfile-optimization-opportunities" ]
  pull_request:
    branches: [ "prod" , "dev" ]

jobs:
  login-dockerhub:
    runs-on: ubuntu-24.04
    outputs:
      username: ${{ steps.get-username.outputs.username_tag }}
    steps:
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        
    - name: Set username output
      id: get-username
      run: echo "username_tag=${{ secrets.DOCKERHUB_USERNAME }}" >> $GITHUB_OUTPUT
    

  
  migrations-service-build:
    needs: login-dockerhub
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        services: [auth,evaluation,event,invitation,project,notification,gateway]
        
    steps:

    - name: Checkout code
      uses: actions/checkout@v3

    - name: test username and repository
      run: |
            echo ${{ secrets.DOCKERHUB_USERNAME }}
            echo ${{ secrets.DOCKERHUB_REPOSITORY }}
        
    - name: Creation of the SHA tag 
      uses: docker/metadata-action@v5
      id: sha-tag
      with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/${{ secrets.DOCKERHUB_REPOSITORY }}
          tags: type=sha,prefix=sha-,suffix=-${{ matrix.services }}
        
    - name: Build and push base image
      uses: docker/build-push-action@v6
      with:
        context: .
        tags: ${{ steps.sha-tag.outputs.tags }}
        push: true

    
    
    
    
